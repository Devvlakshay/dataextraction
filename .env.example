# Document Extraction API Configuration

# Model paths (relative to project directory)
MODEL1_PATH=models/model1.pt
MODEL2_PATH=models/model2.pt

# Directory paths
DOWNLOAD_DIR=workspace/downloads
DATA_DIR=workspace/data

# Processing settings
CONFIDENCE_THRESHOLD=0.15

# Fallback settings
# Number of missing fields to trigger fallback
FALLBACK_THRESHOLD_AADHAAR=2
FALLBACK_THRESHOLD_PANCARD=1

# Ollama settings (for Qwen fallback)
OLLAMA_URL=http://localhost:11434/api/generate
QWEN_MODEL=qwen2.5-vl:7b
